Configure terraform access:
```
export AWS_ACCESS_KEY_ID=AKI......34S
export AWS_SECRET_ACCESS_KEY=uNK....tcO9
export AWS_DEFAULT_REGION=us-west-2
```

Install AWS CLI


Run

aws eks --region us-east-2 update-kubeconfig --name demo-cluster

