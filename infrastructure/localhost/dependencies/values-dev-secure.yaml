hostname: opencrvs.localhost

ingress:
  ssl_enabled: false

redis:
  acl:
    enabled: true

minio:
  # TODO: Revisit naming convention
  # use_default_credentials could be applied to redis and other services as well
  use_default_credentials: false
