apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: k8s-opencrvs-dev-ssl
  namespace: opencrvs-dev
spec:
  # Secret names are always required.
  secretName: k8s-opencrvs-dev-tls
  issuerRef:
    name: cloudflare-issuer
    kind: ClusterIssuer
  dnsNames:
  - "*.k8s.opencrvs.dev"