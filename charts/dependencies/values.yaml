
mongodb:
  enabled: true
  version: 4.4

minio:
  enabled: true
  use_default_credentials: true

influxdb:
  enabled: true

hearth:
  enabled: true

redis:
  enabled: true
  auth_mode: disabled
  # List of placeholders for OpenCRVS "users":
  auth_users:
    - GATEWAY
    - WORKFLOW
    - AUTH
    - WEBHOOKS

elasticsearch:
  enabled: true
  use_default_credentials: true

hostname: farajaland.dev

ingress:
  ssl_enabled: true