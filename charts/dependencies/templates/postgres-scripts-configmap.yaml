apiVersion: v1
data:
{{- range $path, $file := .Files.Glob "files/postgres/*" }}
    {{ base $path }}: |
{{ toString $file | indent 8 }}
{{- end }}
kind: ConfigMap
metadata:
  labels:
    app: postgres
  name: postgres-scripts
