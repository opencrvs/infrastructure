UI installer Todo list:

- Add Helm chart support for providing admin credentials as Helm values/secrets for Elasticsearch, Postgres, and MinIO. This should support an external `values.secrets.yaml` file generated by the configurator when GitHub is not used as the secrets backend, and chart templates should map those secret values into the required datastore credentials.
- Add future support for a GitHub / Ansible bootstrap helper mode: prepare inventory files and the minimal GitHub variables/secrets needed for bootstrap automation without generating Helm values.


General 2.1 fixes:
- ACTIVATE_USERS should be added to data-seed job
